多重继承
1.虚基类可以解决多重继承的两义性问题.
2.抽象类中将虚函数初始化为0，即可设置为纯虚函数，该虚函数并没有任何功能，
其子类在不重新为此虚函数赋予功能时，不能调用。
3.只要一个类中还有函数被初始化为0，则其为抽象类，不要试图去创建一个抽象类的对象。
虚函数
1.静态成员函数不能被说明为虚函数；
2.在继承时将析构函数设置为虚函数，不存在虚构造函数。
静态成员
1.静态成员属于整个类而不是某个对象，所以它在该类的对象中共享；
2.静态成员函数也属于整个类，而且它只能调用该类的静态成员，
不能调用该类的非静态成员，但非静态成员函数却可以调用该类的静态成员。